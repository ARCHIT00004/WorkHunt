{
  "name": "workhunt",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack job board (server in job/server, client in client)",
  "scripts": {
    "dev": "npm --prefix job/server run dev",
    "server": "npm --prefix job/server run dev",
    "client": "npm --prefix client run dev",
    "install:all": "npm --prefix job/server install --no-fund --no-audit && npm --prefix client install --no-fund --no-audit"
  }
}


