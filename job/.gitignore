# Git Ignore File - tells Git which files NOT to track in version control
# This prevents sensitive, temporary, or generated files from being committed to the repository

# Dependencies - these are downloaded by npm/yarn, don't need to be in Git
node_modules/          # All installed packages (can be huge!)
*/node_modules/        # Node modules in subdirectories
npm-debug.log*         # npm error logs
yarn-debug.log*        # Yarn error logs
yarn-error.log*        # Yarn error logs

# Environment variables - contain sensitive information like passwords
.env                   # Main environment file (contains database URLs, JWT secrets, etc.)
.env.local             # Local environment overrides
.env.development.local # Development-specific environment variables
.env.test.local        # Test environment variables
.env.production.local  # Production environment variables

# Keep .env.example files (these are templates without secrets)
!.env.example
!**/.env.example

# Build outputs - these are generated automatically, don't need to be tracked
dist/                  # Vite build output (frontend)
build/                 # Alternative build directory
*/dist/                # Build outputs in subdirectories
*/build/               # Build outputs in subdirectories

# Logs - temporary files that show what happened
logs                   # Log directories
*.log                  # All log files

# Runtime data - temporary files created while app is running
pids                   # Process ID files
*.pid                  # Process ID files
*.seed                 # Random seed files
*.pid.lock             # Process lock files

# Test coverage reports - generated by testing tools
coverage/              # Code coverage reports
*.lcov                 # Coverage data files
.nyc_output            # NYC coverage tool output

# Package manager files
jspm_packages/         # JSPM package directory
.npm                   # npm cache directory
.eslintcache          # ESLint cache for faster linting

# Build tool caches - temporary files to speed up builds
.rpt2_cache/          # Rollup TypeScript cache
.rts2_cache_cjs/      # Rollup cache (CommonJS)
.rts2_cache_es/       # Rollup cache (ES modules)
.rts2_cache_umd/      # Rollup cache (UMD)
.cache                # Parcel bundler cache
.parcel-cache         # Parcel v2 cache

# Node.js specific files
.node_repl_history    # Node.js REPL history
*.tgz                 # npm pack output
.yarn-integrity       # Yarn integrity file

# Framework-specific build outputs
.next                 # Next.js build output
.nuxt                 # Nuxt.js build output
public                # Gatsby public directory (generated)

# Storybook outputs
.out                  # Storybook build output
.storybook-out        # Alternative Storybook output

# Temporary directories
tmp/                  # Temporary files
temp/                 # Temporary files

# Editor and IDE files - these are personal preferences
.vscode/              # Visual Studio Code settings
.idea/                # IntelliJ IDEA settings
*.swp                 # Vim swap files
*.swo                 # Vim swap files
*~                    # Backup files

# Operating system generated files
.DS_Store             # macOS folder settings
.DS_Store?            # macOS folder settings variant
._*                   # macOS resource forks
.Spotlight-V100       # macOS Spotlight index
.Trashes              # macOS trash
ehthumbs.db          # Windows thumbnail cache
Thumbs.db            # Windows thumbnail cache

# Application-specific files
# Uploads directory - keep the folder structure but ignore actual uploaded files
uploads/resumes/*     # Ignore all uploaded resume files (they can be large and personal)
!uploads/resumes/.gitkeep  # But keep the .gitkeep file to preserve the directory structure

# Database files - these contain data and can be large
*.db                  # SQLite database files
*.sqlite              # SQLite database files
*.sqlite3             # SQLite database files

# Testing output
coverage/             # Test coverage reports
.nyc_output/          # NYC test coverage output

# Production builds (duplicate entries for clarity)
build/                # Production build directory
dist/                 # Distribution directory
